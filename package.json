{
  "name": "rp",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://manaenckow.github.io/site/",
  "main": "index.js",
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.4.1",
    "@testing-library/user-event": "^7.2.1",
    "@vkontakte/icons": "^1.16.3",
    "@vkontakte/vk-connect": "^1.9.1",
    "@vkontakte/vkui": "^3.3.7",
    "moment": "^2.24.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.4.0"
  },
  "scripts": {
    "deploy": "gh-pages -d build",
    "minideploy": "vk-miniapps-deploy",
    "start": "cross-env PORT=8080 react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "ssl": "ssh -R 80:localhost:8080 ssh.localhost.run"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^2.2.0"
  }
}
